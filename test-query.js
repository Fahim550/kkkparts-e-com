import { createClient } from '@supabase/supabase-js'

// We need the SUPABASE URL and KEY to run this outside of vite. 
// Can I just do this via vite dev server? No, vite is running but we need a browser context.
